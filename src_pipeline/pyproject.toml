[project]
name = "src-pipeline"
version = "0.1.0"
description = "Pipeline service for consuming and processing IoT MQTT sensor data"
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["paho-mqtt>=2.1.0", "psycopg2-binary", "python-dotenv"]
